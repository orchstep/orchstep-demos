# eng-manager-onboarding

The "OrchStep for engineering managers" demo from the OrchStep blog.

    orchstep run onboard --var name=jordan        # repeatable onboarding
    orchstep run release-check --var tier=production   # the release checklist

Steps are `echo`-only, so the whole thing runs anywhere with no side effects.
